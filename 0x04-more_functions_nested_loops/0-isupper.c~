#include "holberton.h"
#include <stdio.h>

/**
 * int _isupper(int c) - checks if a is capital or not
 *
 * Return: Always 0.
 */

int _isupper(int c)
{
	int i;	
	if (c == 97)

	  i = 0;
	if (c == 65)

	  i = 1;	
	return (i);
}
