#include <stdio.h>

/**
 * main - Entry point
 *
 * prinf: print the characters given
 * Return: Always 0 (Success)
 */
int main(void)
{
	prinf("with proper grammar, but the outcome is a piece of art,/m");
	return (0);
}
